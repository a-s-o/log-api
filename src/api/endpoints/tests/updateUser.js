'use strict';
/* eslint func-names: 0 */
require('co-mocha');
require('should');

const assert = require('assert');
const sinon = require('sinon');

const imports = {

};

const updateUser = require('../updateUser.js')(imports);

describe('updateUser', () => {

   it('should be tested');

});
