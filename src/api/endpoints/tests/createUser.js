'use strict';
/* eslint func-names: 0 */
require('co-mocha');
require('should');

const assert = require('assert');
const sinon = require('sinon');

const imports = {

};

const createUser = require('../createUser.js')(imports);

describe('createUser', () => {

   it('should be tested');

});
